[project]
name = "excalidraw-render"
version = "0.1.0"
description = "Headless PNG renderer for .excalidraw files."
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40",
]

[tool.uv]
package = false
